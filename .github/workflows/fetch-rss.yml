name: Fetch RSS Feeds

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  fetch-rss:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Fetch RSS feeds
        run: python3 .github/workflows/fetch_rss.py

      - name: Commit updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/news.json
          git diff --staged --quiet || git commit -m "RSS update $(date -u '+%Y-%m-%d %H:%M UTC')"
          git push
```

**4.** Page bottom → **"Commit changes"** → **"Commit changes"** (green button)

---

### 🐍 Step 3 — fetch_rss.py file உருவாக்கவும்

**1.** மீண்டும் **"Add file"** → **"Create new file"**

**2.** File name:
```
.github/workflows/fetch_rss.py
